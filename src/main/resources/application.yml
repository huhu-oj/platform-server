# 应用名称
spring:
  application:
    name: platform-server
  profiles:
    active: test
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai
server:
  servlet:
    encoding:
      force: true
      charset: utf-8
      enabled: true
